---
/**
 * Lesson aside. Kinds (docs/04): teoria (just enough theory), mundo-real (in the
 * real world), entrevista (interview question), hoy (how it is done today), roto
 * (broken on purpose).
 */
type Kind = 'teoria' | 'mundo-real' | 'entrevista' | 'hoy' | 'roto';

interface Props {
  kind: Kind;
  title?: string;
}

const { kind, title } = Astro.props;

const labels: Record<Kind, string> = {
  teoria: 'Teoría justa',
  'mundo-real': 'En el mundo real',
  entrevista: 'Pregunta de entrevista',
  hoy: 'Así se hace hoy',
  roto: 'Roto a propósito',
};
---

<aside class:list={['callout', `callout--${kind}`]} aria-label={labels[kind]}>
  <p class="callout__head">
    <span class="label callout__kind">// {labels[kind]}</span>
    {title && <span class="callout__title">{title}</span>}
  </p>
  <div class="callout__body">
    <slot />
  </div>
</aside>

<style>
  .callout {
    --callout-color: var(--brand);
    position: relative;
    margin-top: 1.6em;
    padding: 18px 20px 18px 24px;
    border: 1px solid var(--line);
    border-left: 3px solid var(--callout-color);
    background: var(--frame-bg);
  }

  .callout--teoria {
    --callout-color: var(--brand);
  }

  .callout--mundo-real {
    --callout-color: var(--ok);
  }

  .callout--entrevista {
    --callout-color: var(--accent-text-aa);
  }

  .callout--hoy {
    --callout-color: var(--event-text-aa);
  }

  .callout--roto {
    --callout-color: var(--ink);
    border-style: dashed;
    border-left-style: solid;
  }

  .callout__head {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 6px 14px;
    margin-bottom: 8px;
  }

  .callout__kind {
    color: var(--callout-color);
  }

  .callout--roto .callout__kind {
    color: var(--ink);
  }

  .callout__title {
    font-weight: 600;
    color: var(--ink);
  }

  .callout__body {
    font-size: 0.95em;
  }

  .callout__body > :global(* + *) {
    margin-top: 0.8em;
  }
</style>
